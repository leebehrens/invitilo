#!/usr/bin/env bash

invitilo_prompt() {
    # shellcheck disable=SC2034
    # PROMPT="${NEWLINE}${BOLD}${CYAN}Invitilo${RESET} ${BOLD}${WHITE}▸ ${BLACK}\$${RESET} "
    PROMPT="${NEWLINE}💻 ${BOLD}${GREEN}\u@\h${RESET}:${BOLD}${BLUE}\w${RESET}\$ "
}
