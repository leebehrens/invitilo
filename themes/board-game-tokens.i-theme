#!/usr/bin/env bash



# shellcheck disable=SC2034
invitilo_theme_init() {
    VENV_PREFIX="${YELLOW}◲${ITALIC}"
    VENV_SUFFIX="${RESET} "
    GIT_BRANCH_PREFIX=" ${RED}🗐 ${ITALIC}" # ⛙ Ⲗ
    GIT_BRANCH_SUFFIX="${RESET}"
    SUCCESS_STATUS=""
    ERROR_STATUS=""
}

# shellcheck disable=SC2034
invitilo_theme_prompt() {
    PROMPT="${NEWLINE}${VENV}${BOLD}${GREEN}🖳 ${ITALIC}\u@\h${RESET} ${BOLD}${BLUE}🖬 ${ITALIC}\w${RESET}${GIT_BRANCH}\n${ITALIC}${BOLD}${BLACK}\$${RESET} "
}
