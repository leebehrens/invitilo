#!/usr/bin/env bash

# GIT_BRANCH_WRAPPER="<${GIT_BRANCH}>"

# shellcheck disable=SC2034
invitilo_theme_prompt() {
    PROMPT="${NEWLINE}${BOLD}${RED}Invitilo${RESET} ${BOLD}${BLACK}${GIT_BRANCH}${RESET} ${BOLD}${WHITE}▸ ${BLUE}\$${RESET} "
}
